<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.rocketkurly.admin.dao">
	
	<insert id="insertCoupon" parameterType="java.util.HashMap">
		insert into cupon(coupon_no, member_id, coupon_name, p_date, e_date, discount)
		values(#{coupon_no}, #{member_id}, #{coupon_name}, sysdate, #{e_date}, #{discount})
   </insert>

</mapper>