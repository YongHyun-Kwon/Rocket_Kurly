<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.rocketkurly.admin.dao">
	
	<insert id="enroll" parameterType="ItemVO">
		insert into item(item_no, name, price, expired, detail, main_img, final_price, sales, c_category_id)
		values(#{item_no}, #{ name }, #{ price }, #{ expired }, #{ detail }, #{ main_img }, #{ final_price }, #{ sales },
		 #{c_category_id})
	</insert>
	
	<select id="selectItem" resultType="String" parameterType="String">
		select member_id
		from member
		where member_id=#{ id }
	</select>

</mapper>